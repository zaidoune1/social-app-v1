import { likes } from "../likes";

export const addComment = (like: likes): void => {};

export const getOneComment = (id: number): likes | void => {};

export const getAllComment = (likes: likes[]): likes[] | void => {};

export const deleteComment = (like: likes): string | void => {};

export const updateComment = (like: likes): likes | void => {};
