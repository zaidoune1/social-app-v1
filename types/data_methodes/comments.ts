import { comments } from "../comments";

export const addComment = (comment: comments): void => {};

export const getOneComment = (id: number): comments | void => {};

export const getAllComment = (comments: comments[]): comments[] | void => {};

export const deleteComment = (comment: comments): string | void => {};

export const updateComment = (comment: comments): comments | void => {};
